<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <!-- PostgreSQL(本番と同じDB製品)でテストを実行するための設定。
         通常のテスト(phpunit.xml)はSQLiteのインメモリDBで高速に実行するが、行ロック等の挙動はDB製品ごとに異なるため、
         同時実行制御のテスト(tests/Concurrency)と、E2Eテスト(tests/Browser、Pestのブラウザテスト)はこちらでのみ実行する。
         実行方法はREADME.mdの「テストの実行」を参照。
         接続先ホスト等(DB_HOST・DB_USERNAME・DB_PASSWORD)は.envまたは環境変数の値を使う。 -->
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
        <testsuite name="Concurrency">
            <directory>tests/Concurrency</directory>
        </testsuite>
        <testsuite name="Browser">
            <directory>tests/Browser</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <!-- force="true" is required because docker-compose injects .env (APP_ENV=local) as real
             container env vars, which PHPUnit's <env> would otherwise not override. -->
        <env name="APP_ENV" value="testing" force="true"/>
        <!-- 画面の文言は本番と同じく日本語(lang/ja.json)で検証する -->
        <env name="APP_LOCALE" value="ja" force="true"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file" force="true"/>
        <env name="BCRYPT_ROUNDS" value="4" force="true"/>
        <env name="BROADCAST_CONNECTION" value="null" force="true"/>
        <env name="CACHE_STORE" value="array" force="true"/>
        <env name="DB_CONNECTION" value="pgsql" force="true"/>
        <env name="DB_DATABASE" value="meethub_testing" force="true"/>
        <env name="DB_URL" value="" force="true"/>
        <env name="MAIL_MAILER" value="array" force="true"/>
        <env name="QUEUE_CONNECTION" value="sync" force="true"/>
        <env name="SESSION_DRIVER" value="array" force="true"/>
        <env name="PULSE_ENABLED" value="false" force="true"/>
        <env name="TELESCOPE_ENABLED" value="false" force="true"/>
        <env name="NIGHTWATCH_ENABLED" value="false" force="true"/>
    </php>
</phpunit>
